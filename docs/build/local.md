# Local Build

## Compilation
- `gcc -O2 -o main main.c`
- Static linking is optional; container build uses `-static`.

## Execution
- Ensure `main.html` exists at the configured `CONTENT_PATH` or in the working directory if using the default.
