# Run

- Default `CONTENT_PATH` is `/main.html` (absolute). For local runs, set `CONTENT_PATH=main.html` if you want to serve the file from the current working directory.
- Ensure the content file exists before starting the server.
